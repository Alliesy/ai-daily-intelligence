{
  "name": "ai-daily-intelligence",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "pnpm --filter @ai-daily/importer build",
    "lint": "pnpm --filter @ai-daily/importer typecheck",
    "typecheck": "pnpm --filter @ai-daily/importer typecheck",
    "test": "node supabase/tests/verify-foundation.mjs && pnpm --filter @ai-daily/importer test"
  }
}
